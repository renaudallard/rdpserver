# build artefacts
*.o
*.a
*.d
config.mk
src/include/config.h

# binaries
src/daemon/rdpd
src/sessionmgr/rdp-sessionmgr
src/session/rdp-session
src/session/rdp-cups-backend

# regress + fuzz
regress/common/*_test
regress/wire/*_test
regress/integ/*_live
regress/fuzz/fuzz_parsers

# anything dropped under tmp/ at runtime (certs, test scratch, logs)
tmp/*
!tmp/.gitkeep

# reverse-engineering scratch
tmp_ghidra/
Windows App.app/

# editor / tagger
tags
TAGS
.depend
src/ddx/rdpserverdev_drv.so
src/session/protocols/
*.xz
*.dsc
tools/rdp-passwd

# Harness scratch (never commit)
.claude/
